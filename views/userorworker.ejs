<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select User Type</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #F2F0EF; /* replace with your image path */
            background-size: cover;
            background-position: center;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-attachment: fixed; /* Makes the background image fixed when scrolling */
            color: #333;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.95);
            padding: 30px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 5px 30px #351E10;
            max-width: 600px;
            width: 100%;
            
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .container:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 40px #351E10;
        }

        h2 {
            margin-bottom: 30px;
            font-size: 28px;
            color: #351E10;
        }

        .options {
            display: flex;
           
            gap: 24px; 
            justify-content: space-between;
            margin-top: 20px;
            padding: 20px;
        }

        .option {
            width: 45%;
            padding: 30px;
            border: 2px solid #ddd;
            border-radius: 10px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
            text-align: center;
            background: linear-gradient(145deg, #f1f1f1, #e0e0e0);
            box-shadow: 4px 4px 10px #351E10;
        }

        .option:hover {
            background-color: black;
            box-shadow: 6px 6px 12px #351E10;
            transform: scale(1.1);
        }

        .option img {
            max-width: 80px; /* Increased icon size */
            margin-bottom: 15px;
            transition: transform 0.3s ease;
        }

        .option:hover img {

            transform: scale(1.2.5); /* Slightly enlarges the image on hover */
        }

        p {
            font-size: 20px;
            color: #351E10;
            margin-top: 5px;
        }

        /* Optional button to improve interaction on smaller screens */
        @media (max-width: 768px) {
            .options {
                flex-direction: column;
            }

            .option {
                width: 100%;
                margin-bottom: 20px;
            }

            .option:last-child {
                margin-bottom: 0;
            }
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>What best represents you?</h2>
        <div class="options">
            <div class="option" onclick="location.href='/usersignup'">
                <img src="/homeowner.png" alt="User Icon"> <!-- Replace with actual icon -->
                <p>Home Owner</p>
            </div>
            <div class="option" onclick="location.href='/workersignup'">
                <img src="/worker.png" alt="Worker Icon"> <!-- Replace with actual icon -->
                <p>Constructor</p>
            </div>
        </div>
    </div>

</body>
</html>
